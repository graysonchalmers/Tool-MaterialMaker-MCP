# Secrets / config
.env
.mcp.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Render output / working files
catalog/catalog.json
output/
*.tmp.ptex
run.log

# Disposable live-control overlay: ensure_overlay() copies the full Material
# Maker checkout (~266 MB, incl. a nested .git) here on first live-control use.
mm_live_overlay/

# Phase 3 quality: keep test_set + scorecards; ignore heavy renders and the
# authored .ptex variants (author.py regenerates them deterministically).
quality/runs/
quality/authored/

# Cookbook growth (informal, non-frozen extensions like the fabric cases):
# same story, cookbook_fabrics.py regenerates the .ptex variants.
quality/cookbook/

# OS junk
Thumbs.db
Desktop.ini
.DS_Store
